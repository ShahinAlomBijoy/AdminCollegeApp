<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admincollegeapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/collegepic"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdminCollegeApp">
        <activity android:name=".faculty.UpdateTeacherActivity"></activity>
        <activity android:name=".faculty.UpdateFaculty" />
        <activity android:name=".faculty.AddTeacher" />
        <activity android:name=".UploadPdf" />
        <activity android:name=".Upload_Image" />
        <activity android:name=".Upload_Notice" /> <!-- <activity android:name=".MainActivity"> -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>